Automatic Security Updates enabled?
• In the file /etc/apt/apt.conf.d/20auto-upgrades

        RH: systemctl status yum-cron.service
        DEB: /etc/apt/apt.conf.d/50unattended-upgrades


kamil
[4:48 PM] 
• cat /etc/hosts
• cat /etc/hostname


[4:49] 
• RH: sudo ntpd (wait 5 mins)
    • ntpq -pn


[4:50] 
RH: /etc/yum.repos.d/


[4:50] 
(anstatt /etc/apt)


kamil [5:05 PM] 
#### check common commands ####
hostname
sudo su
init 6

uptime

#### Virtual console memory ####
• sudo cat /dev/vcs1
• cat /etc/hosts
• cat /etc/hostname

#### Check release ####
lsb_release -a (deb)
hostnamectl    (rpm)

#### Check the file ####
*** deb
cat /etc/apt/sources.list

*** yum
cat /etc/yum.conf
cat /etc/yum.repos.d/centos7-latest.repo (SWITCH)
cat /etc/redhat-release
ll /etc/yum.repos.d/*

• correct Release
• mirrors in Switzerland
• security repo enabled
    

#### automatic Security Updates enabled? ####
cat /etc/apt/apt.conf.d/20auto-upgrades
OUTPUT:
APT::Periodic::Update-Package-Lists “1”;
APT::Periodic::Unattended-Upgrade “1";

   *** yum:
    systemctl status yum-cron.service
    (systemctl status yum-cron.service)

#### Check ####
apt-config dump | grep Periodic
OUTPUT:
APT::Periodic “”;
APT::Periodic::Update-Package-Lists “1";
APT::Periodic::Download-Upgradeable-Packages “0”;
APT::Periodic::AutocleanInterval “0";
APT::Periodic::Unattended-Upgrade “1”;

#### Check the file ####
cat /etc/ntp.conf
OUTPUT: 
Pool in Switzerland
server 0.ch.pool.ntp.org
server 1.ch.pool.ntp.org
server 2.ch.pool.ntp.org
server 3.ch.pool.ntp.org


#### Time synchro ####
ntpq -p
    • RH: sudo ntpd (wait)
    • ntpq -pn


ERKLÄRUNG
remote: peers specified in the ntp.conf file
* = current time source
# = source selected, distance exceeds maximum value
o = source selected, Pulse Per Second (PPS) used
+ = source selected, included in final set
x = source false ticker
. = source selected from end of candidate list
– = source discarded by cluster algorithm
blank = source discarded high stratum, failed sanity